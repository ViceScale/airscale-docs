---
title: "People URL finder"
description: "Find a professional profile URL from person and company details. Rate limit: 6 requests per second per workspace."
canonical: "https://docs.airscale.io/api-reference/people-url-finder"
openapi: "/openapi.json POST /v1/url-search-people"
---

Identify a person by first name, last name, and company, then return a matching professional profile URL when one is found.

<Note>
The limit is 6 requests per second per workspace. A successful result costs 0.5 credits; `not_found` is not charged.
</Note>

After a `429 Too Many Requests` response, retry in a later second with bounded exponential backoff. For temporary `502` or `503` responses, retry the same input with increasing delays and a fixed retry limit; contact support if errors persist.

## Next step

Pass the matched URL to [Email finder](/api-reference/email-finder) for professional email enrichment.
